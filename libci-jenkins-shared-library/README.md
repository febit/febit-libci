## Febit LibCI - Jenkins Shared Library

This module is designed to organize the code sources related to Jenkins Shared Library in the form of a Gradle module,
providing dependency management, compilation checks, unit testing, and other functions.
The main code is located in the `src` and `vars` directories of the **project root directory**.
